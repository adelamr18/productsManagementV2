export interface UserShipmentDetails {
    firstName: string;
    lastName: string;
    address: string;
    mobile: string;
}

export interface UserPaymentDetails {
    cardHolder: string;
    iban: string;
}